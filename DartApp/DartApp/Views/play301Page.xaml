<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="DartApp.Views.play301Page">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Throw score:"/>
            <Entry x:Name="throwEntry" HorizontalOptions="FillAndExpand"/>
            <Button x:Name="addScoreButton" Text="add score" Command="{Binding addScore}" CommandParameter="{Binding TurnScore}"/>
            <Label Text="{Binding TurnScore, Mode=TwoWay}"/>
            <Label Text="{Binding TotalGameScore, Mode=TwoWay}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>